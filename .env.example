# Copy these values into the current PowerShell session; do not commit real secrets.
$env:WIDE_RTSP_URL = "rtsp://user:password@wide-host:554/path"
$env:PTZ_PASSWORD = "replace-me"
# Optional when the default PTZ RTSP path is not /0:
# $env:PTZ_RTSP_URL = "rtsp://user:password@ptz-host:554/path"
